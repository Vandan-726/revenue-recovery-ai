{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "node ./scripts-dev/dev.mjs",
    "dev:api": "tsx watch artifacts/api-server/src/index.ts",
    "dev:web": "pnpm --filter @workspace/revenue-recovery-dashboard run dev",
    "db:push": "pnpm --filter @workspace/db run push-force",
    "smoke-test": "node scripts/api-smoke-test.mjs",
    "test:security": "node scripts/security-test.mjs",
    "test:security:advanced": "node scripts/advanced-security-test.mjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "tsx": "catalog:",
    "typescript": "~5.9.3"
  }
}
